spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/silviuStefan/cloud-global
          search-paths: config-data
  application:
    name: config-service
server:
  port: 8888